trigger:
  branches:
    include:
      - main

pool:
  name: "Hosted Agent"

steps:
  - task: DownloadPipelineArtifact@2
    inputs:
      buildType: "specific"
      project: "gitleak"
      pipeline: "gitleak"
      artifact: "RepoBArtifact"
