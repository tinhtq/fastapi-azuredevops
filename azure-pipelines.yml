trigger:
  branches:
    include:
      - main

pool: myagent

steps:
  - task: DownloadPipelineArtifact@2
    inputs:
      buildType: "specific"
      project: "gitleak"
      pipeline: "gitleak"
      artifact: "RepoBArtifact"
